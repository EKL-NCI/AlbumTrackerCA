<p style="color: green"><%= notice %></p>

<% content_for :title, "Albums" %>

<h1>Albums</h1>
<p>Total albums: <%= @albums.count %></p>

<div id="albums">
  <% @albums.each do |album| %>
    <%= render album %>
    <p>
      <%= link_to "Show this album", album %>
    </p>
  <% end %>
</div>

<%= link_to "New album", new_album_path %>
