<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Album Tracker" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Application SCSS -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg bg-body-tertiary mb-4">
      <div class="container-fluid">
        <!-- Logo + Brand Text -->
        <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
          <%= image_tag "logo-placeholder-image.png", alt: "Logo", width: 60, height: 48, class: "d-inline-block align-text-top me-2" %>
          Album Tracker
        <% end %>

        <!-- Toggler for mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar links + search -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <!-- Links aligned right -->
          <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to "Add New", new_album_path,
                  class: "d-inline-flex focus-ring focus-ring-success py-1 px-2 text-decoration-none border rounded-2" %>
            </li>
          </ul>

          <!-- Right-aligned search bar -->
          <%= form_with url: albums_path, method: :get, class: "d-flex ms-3", local: true do |f| %>
            <%= f.search_field :q, placeholder: "Search", class: "form-control me-2", aria: { label: "Search" } %>
            <%= f.submit "Search", class: "btn btn-outline-success" %>
          <% end %>
        </div>
    </nav>

    <!-- Flash messages -->
    <div class="container">
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type == 'notice' ? 'success' : 'danger' %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
    </div>

    <!-- Main content -->
    <div class="container mt-4">
      <%= yield %>
    </div>
  </body>
</html>
